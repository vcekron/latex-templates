\ProvidesPackage{STY/DEFINITIONS}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
% N E W   C O M M A N D S
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
% Define a fsize command for fetching current font size.
\makeatletter
\newcommand*\fsize{\dimexpr\f@size pt\relax}
\makeatother

% Some useful newcommands
\newcommand\overlayChar[1]{{\sffamily\textbf{#1}}} % Image overlay creator (A, B, C in boxes etc).
\newcommand\sublabelChar[1]{\textbf{(#1)}} % Add sublabel to figures [a), b), c) etc].
\newcommand\tageq[1]{\tag{\hypersetup{linkcolor={black}}#1}} % Tag equation, e.g. \tageq{\eqref{eq:farts}}.
\newcommand{\DeG}{$^\circ$}
\newcommand{\me}{\mathrm{e}} % Properly define exponential 'e' in math environment.
\newcommand{\temptext}[1]{{\color{purple}#1}} % Quickly colour temporary text using \temptext{}.

% Define straight integral signs via \uint and \uoint.
\DeclareMathOperator*{\uint}{\scalerel*{\rotatebox{6}{$\!\textstyle\int\!$}}{\int}}
\DeclareMathOperator*{\uoint}{\scalerel*{\rotatebox{6}{$\!\textstyle\oint\!$}}{\oint}}

% Giant brackets.
\makeatletter
\newcommand{\giant}{\bBigg@{4}}
\newcommand{\Giant}{\bBigg@{5}}
\makeatother

\endinput
